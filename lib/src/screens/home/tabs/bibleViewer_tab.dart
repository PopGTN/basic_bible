import 'package:flutter/material.dart';

class BibleViewerTab extends StatelessWidget {
  const BibleViewerTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('bible'));
  }
}
